version: "2"
linters:
  default: all
  enable:
    - gomodguard_v2
    - wsl_v5
  disable:
    - depguard
    - exhaustruct
    - gochecknoglobals
    - gocognit
    - gomodguard
    - wrapcheck
    - varnamelen
    - err113
    - lll
    - cyclop
    - funlen
    - maintidx
    - wsl
formatters:
  # Enable specific formatter.
  # Default: [] (uses standard Go formatting)
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
    - golines
