version: "2"
linters:
  default: all
  enable:
    - wsl_v5
  disable:
    - depguard
    - exhaustruct
    - gochecknoglobals
    - gocognit
    - wrapcheck
    - varnamelen
    - err113
    - lll
    - cyclop
    - funlen
    - maintidx
    - wsl
formatters:
  # Enable specific formatter.
  # Default: [] (uses standard Go formatting)
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
    - golines
